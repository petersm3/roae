# GATE 5 allowlist — lines where a canonical quantity legitimately appears alongside the
# OPPOSITE status word, because the sentence is COMPARING exact against estimate rather
# than mislabelling anything. Format: one "path:line" per line, with the reason above it,
# followed by a TAB and a literal substring identifying the reviewed sentence.
#
# THE ANCHOR IS THE KEY, NOT THE LINE NUMBER. Supply one always. The gate matches an entry
# by (file, anchor); the line number is documentation, and the gate reports when it goes
# stale. An entry with no anchor falls back to line-number matching and is reported as
# drift-prone every run.
#
# Why (2026-08-01, learned twice in one day): line numbers do not survive editing, and the
# failure is not symmetric. The TR-4 entry was written at :119, two rows were inserted above
# it, and it then both failed to suppress the row it was written for AND silently covered
# whatever had moved into :119 — an unreviewed line inheriting someone else's suppression.
# The HISTORY.md entry below repeated it: correct at :5563 when written, pushed to :5566
# hours later by an insertion three lines above. Matching on content makes the dangerous
# direction impossible (suppression requires the reviewed text) and stops the harmless
# direction from raising a WARN that carries no information.
#
# Reviewed 2026-08-01. Both entries verified by reading the sentence, not by pattern.

# States the ratio of the EXACT orbit count to the Knuth ESTIMATE (0.999956) — the
# comparison is the point of the sentence. (Recorded line refreshed :5563 -> :5566 on
# 2026-08-01; the sentence is unchanged, an unrelated edit inserted 3 lines above it.)
documentation/HISTORY.md:5568	ratio to the Knuth estimate is **0.999956**

# The estimator-calibration table row for C1-C5, whose "exact" cell literally reads
# "none — no exact value exists". The word "exact" is there to deny an exact value, and
# the 1.3287e38 in the row sits under a column headed "Prior Knuth estimate" — the status
# is correct, the gate is reading a negation. (Re-anchored from :119 to :121 on 2026-08-01
# after two lines were inserted above it; anchor added so it cannot drift silently again.)
reports/TR4_SIZE_OF_THE_SPACE.md:123	*none — no exact value exists*
